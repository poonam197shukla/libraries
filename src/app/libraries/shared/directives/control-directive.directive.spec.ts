import { ControlDirectiveDirective } from './control-directive.directive';

describe('ControlDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new ControlDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
